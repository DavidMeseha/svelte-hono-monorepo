{
	"version": 2,
	"builds": [
		{
			"src": "src",
			"use": "@vercel/bun"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/index.ts"
		}
	],
	"buildCommand": "npx prisma generate && pnpm run build",
	"installCommand": "pnpm install"
}
